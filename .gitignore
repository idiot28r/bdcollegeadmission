# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.vercel

# Claude Code session state and worktrees
.claude/

# Supabase CLI cache (project ref, etc. — not for commit)
supabase/.temp/

# One-off DB audit / analysis / migration scripts and their JSON+text logs.
# Executed as needed; kept locally only as a paper trail, not for the repo.
_*
*.mjs
/mislabelled_report.json
/qa_review_needed.json
/sme_review_report.json
